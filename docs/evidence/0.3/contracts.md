# Phase 0.3 contract evidence

The implementation exposes only the documented settings, bundle, diagnostics,
and CLI surfaces. ETLantic owns all control-plane models, routes, stores, and
schema migrations. No secrets, URLs, machine paths, timestamps, or exception
representations are included in diagnostic output.
