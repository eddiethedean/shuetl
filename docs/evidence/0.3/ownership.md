# Phase 0.3 ownership evidence

The host owns authentication adapters, authorization policy, application
lifespan, and bundle cleanup. ShuETL owns validation, compatibility checks,
provider composition, redacted diagnostics, and packaging metadata. Upstream
ETLantic packages own persistence schema, migration provisioning, API routes,
and execution semantics.
